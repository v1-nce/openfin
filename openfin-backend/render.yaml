services:
  - type: web
    name: openfin-backend
    env: python
    rootDir: openfin-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT

